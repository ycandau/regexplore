import { createTheme } from '@material-ui/core';

export default createTheme({
  palette: {
    type: 'dark',
    custom: {
      green: '#4ad14a',
      orange: '#ffa500',
      darkGreen: '#006020',
      red: '#ff0000',
      background: '#212121',
    },
  },
});
