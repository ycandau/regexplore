import { createTheme } from '@material-ui/core';

export default createTheme({
  palette: {
    type: 'light',
    custom: {
      green: 'rgb(74, 209, 74)',
      orange: 'rgb(255, 196, 0)',
    },
  },
});
