import { createTheme } from '@material-ui/core';

export default createTheme({
  palette: {
    type: 'light',
    custom: {
      green: 'rgb(74, 209, 74)',
      orange: '#ffa500',
      darkGreen: '#006020',
    },
  },
});
