import { createTheme } from '@material-ui/core';

export default createTheme({
  palette: {
    type: 'light',
    custom: {
      green: '#32aD32',
      orange: '#ffa500',
      darkGreen: '#006020',
      background: '#ffffff',
    },
  },
});
